apiVersion: v1
kind: Pod

metadata:
  name: resources-demo

spec:
  containers:
    - name: nginx
      image: nginx
      env:
      - name: project
        value: loreal
      - name: cource
        value: Devops